## Tar

Compress directory
```bash
tar -zcvf <file_name>.tar.gz <target_directory>/ 
```

Decompress archive
```bash
tar -zxvf <file_name>.tar.gz -C <target_directory>
```

See what's inside
```bash
tar -tvf <file_name>
```
