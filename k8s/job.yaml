---
apiVersion: batch/v1
kind: Job
metadata:
  name: myjob
spec:
  completions: 3
  parallelism: 3
  template:
    spec:
      containers:
      - name: myapp-container
        image: my-app-image
      restartPolicy: Never
  backoffLimit: 10
